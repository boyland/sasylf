package regression.bad2

module test provides

terminals T F

syntax b ::= T | F

judgment lor : b \/ b = b

  --------- lorrule1
  T \/ b = T

  --------- lorrule2
  F \/ b = b

module m = regression.bad2.functor[lor] //! because it expects a syntax

module m2 = regression.bad2.functor[m] //! because module is an invalid argument

module m3 = regression.bad2.functor[b] // this is fine